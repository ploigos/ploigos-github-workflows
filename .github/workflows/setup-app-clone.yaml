name: Setup App Clone

on:
  workflow_call:

jobs:
  setup-app-clone:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: setup-app-clone
        run: |
          echo 'Setup App Clone'