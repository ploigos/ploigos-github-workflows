name: Setup App Clone

on:
  workflow_call:
    inputs:
      verbose:
        required: false
        type: string
      image:
        required: true
        type: string
      imagePullPolicy:
        required: true
        type: string
      url:
        required: true
        type: string
      revision:
        required: true
        type: string
      deleteExisting:
        required: true
        type: string
      sslVerify:
        required: true
        type: string
      refspec:
        required: true
        type: string

jobs:
  setup-app-clone:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: setup-app-clone
        run: |
          echo 'Setup App Clone'
